# GitHub (for creating user project repos)
GITHUB_TOKEN=ghp_...

# Agent configuration
# All agents run via the `claude` CLI and consume your Claude subscription
# (Max plan). No Anthropic API key is required.
MAX_CONCURRENT_AGENTS=3
AGENT_MODEL=claude-sonnet-4-6

# Server
BACKEND_PORT=8000
FRONTEND_PORT=5173

# Dev-only dependencies — install with: pip install -r backend/requirements-dev.txt
# (keeps the base requirements.txt lean for production/CI runtime)
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.27.2        # FastAPI test client
ruff==0.7.4          # lint + format
